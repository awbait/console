## Правила для Claude

- **Никогда не добавлять `Co-Authored-By` в коммиты.**
- **Никогда не добавлять `Generated with Claude Code` в PR.**
- **Сообщения коммитов - только на английском** (заголовок и тело), Conventional Commits.
- **Не использовать длинное тире (em dash `—`) нигде** - ни в коде, ни в Markdown, ни в issue, PR, комментариях, ни в ответах. Вместо него: обычный дефис `-`, двоеточие или перестроить фразу.
- **Отвечать на русском.** Все ответы, комментарии и пояснения - на русском языке.
- **Комментарии в коде - только на английском.** Русский язык допустим только в Markdown-файлах (`.md`), issue и PR (но НЕ в сообщениях коммитов). В исходниках (Go, TS, Python, Makefile, конфиги) - никакой кириллицы: редакторы и Gitea подсвечивают её как ambiguous Unicode.
- **Проверять работу до коммита самому** (сборка, тесты, `tsc`). Lefthook на `git push` - бэкстоп, а не замена локальной проверки.
- **Удалять артефакты сборки.** Если `go build` или другая команда создаёт бинарники (`.exe`, бинарные файлы), удалять их после проверки. Не оставлять в рабочей директории.

## Скиллы

| Скилл | Когда вызывать |
|---|---|
| `git-workflow` | Любая работа с git: ветка, коммит, PR, релиз. Единственный источник правды по процессу. |
| `changelog` | Запись в журнал изменений и выпуск версии. |
| `ui-ux-pro-max` | Оформление интерфейса: страница, компонент, вёрстка, цвет, типографика, анимация, доступность. |
| `playwright-cli` | Посмотреть на страницу в браузере: как выглядит, что в консоли, что уходит в API. |

Скиллы описывают процесс; при расхождении с этим файлом старший источник - этот
файл (табу выше).

## Остальные правила

Лежат в `.claude/rules/` и подхватываются сами, когда я открываю подходящие файлы:

| Файл | О чём | Когда грузится |
|---|---|---|
| `frontend.md` | инструменты фронтенда, `bun`, дев-стенд и вход | `web/**` |
| `ux-copy.md` | тексты интерфейса: тон, шаблоны ошибок | `web/**` |
| `config.md` | переменные окружения и их описания | `internal/config/**`, `.env.example` |
| `observability.md` | логи и метрики | `**/*.go` |
