# Dev-only: the backend in AUTH_MODE=dev reads these to impersonate a user.
# In OIDC mode they are ignored (cookies/session used instead).
VITE_DEV_AUTH=true
VITE_DEV_TEAMS=core
VITE_DEV_ROLE=member
