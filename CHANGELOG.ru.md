# Список изменений

Все значимые изменения проекта фиксируются в этом файле.

## [Unreleased]

### Изменено
- Журнал изменений на странице «О портале» раскрывается по одной версии.
  Последняя открыта, остальные свёрнуты и занимают по строке: номер и дата.
  Версия, которая сейчас работает, помечена «Сейчас в проде». Когда вы
  раскрываете версию, список подводит её заголовок к верху, чтобы читать
  с начала.
- Внутри раскрытой версии «Добавлено», «Изменено» и «Исправлено» разделены
  цветом, каждая категория своим.
- Вкладка «Изменения» у чарта показывает пять последних версий, остальные
  открываются кнопкой. Пока вы читаете релиз, его версия остаётся видна над
  списком.
- «Описание» и «Изменения» на странице чарта стали вкладками одной карточки.
  Раньше это была полоса вкладок над отдельной рамкой во весь экран, пустой
  на три четверти. Карточка занимает столько высоты, сколько нужно тексту,
  а описание читается в колонку, а не во всю ширину монитора.
- Ссылка на страницу чарта запоминает, что вы читали: `?tab=changelog`
  открывает «Изменения», а `#release-2.3.0` открывает нужную версию и подводит
  к ней список.
- Страница «О портале» занимает всю ширину окна. Раньше она держала свою
  ширину, и со свёрнутым меню половина экрана оставалась пустой.

### Исправлено
- Версия, под которой ничего нет, больше не показывается. Раньше сразу после
  релиза журнал начинался с пустого блока «Готовится к выпуску».
- Текст в журнале изменений больше не сужается, когда версия раскрывается:
  полоса прокрутки держит свою колонку, а не отбирает ширину у записей
  в момент появления.

## [0.6.0] - 2026-08-20

Релиз про конструктор версии. Документ больше не пишут в пустое поле: редактор
подсказывает поля чарта, объясняет ключи и сам сохраняет черновик.

### Добавлено
- Конструктор версии сохраняет черновик сам, через пару секунд после того, как
  вы перестали печатать, и ещё раз при уходе со страницы. Рядом с кнопкой
  «Сохранить черновик» написано, где сейчас работа: сохранена, сохраняется или
  не сохранена. Закрыть вкладку с несохранёнными правками браузер не даст молча.
- Конструктор версии подсказывает по ходу письма. Ctrl+Space открывает список
  того, что уместно на месте курсора: ключи документа и поля этой версии чарта с
  теми названиями, которые дал им чарт. Там, где из набранного остаётся одно
  поле, остаток показан серым и принимается по Tab.
- Наведение на ключ в конструкторе версии объясняет, зачем он нужен, а ошибка
  подчёркивается на своей строке, а не только перечисляется под редактором.

### Изменено
- Портал открывается быстрее: первая загрузка стала легче на четверть. Страницы,
  которые открывают редко, теперь загружаются в момент открытия: разделы
  администратора, ИБ и поддержки, документация, конструктор версии.
- Меню слева сворачивается само, когда окно уже 1024 пикселей: страница получает
  больше 200 пикселей, которые на узком экране нужнее ей. Свёрнутое меню и само
  стало уже. Развернуть его можно и там, кнопкой в его нижней части. На широком
  экране портал по-прежнему помнит ваш выбор.

### Исправлено
- Поле формы объясняет отказ тем правилом, которое нарушено. Раньше значение
  `-abc` в имени получало ответ про допустимые символы, хотя символы верные, а
  дело в дефисе с краю.
- Поля, которые начинаются с буквы, и поля с путём теперь тоже подсказывают
  формат. До этого они молчали, а на ошибку отвечали «Недопустимый формат».
- В форме заказа удаление блока из середины списка больше не сбивает соседние.
  Раскрытым остаётся тот блок, который вы открыли, а не тот, что занял его
  место.
- На экране входа кнопка «Войти через Keycloak» гаснет, пока портал не отвечает.
  Раньше нажатие уводило на страницу с ошибкой браузера, из которой не было пути
  назад. Теперь под кнопкой написано, что портал не отвечает и что попробовать.

## [0.5.0] - 2026-08-19

Релиз про уведомления. Портал сам сообщает, что случилось с вашим сервисом,
заказом или версией, а не ждёт, пока вы откроете нужную страницу.

### Добавлено
- Уведомления. Портал присылает их, когда с вашим сервисом или заказом
  что-то произошло. В шапке появился колокольчик: пока есть непрочитанное, на
  нём горит точка и он покачивается. Вся история лежит на странице
  «Уведомления», там она разложена по дням, а чипы над списком показывают либо
  всё, либо только непрочитанное.
- Уведомление приходит, когда заказанный сервис развернулся, перестал работать
  или не смог принять ваше изменение. По нажатию открывается страница заказа.
- Команда, которая публикует сервис, получает уведомление о решении по своей
  версии: согласована или отклонена. В отказе сразу виден комментарий
  согласующего, раньше за ним нужно было идти на страницу версии.
- Если опубликованная версия пропала из реестра, портал сообщает об этом
  владеющей команде и администратору. Заказать такую версию нельзя, а сервис в
  каталоге выглядит так, будто его никто не публиковал. По нажатию открывается
  страница этой версии.
- Администратор получает уведомление, когда версию сервиса отправили на
  согласование. По нажатию открывается страница, где её согласуют. Раньше об
  очереди можно было узнать, только открыв раздел согласований.
- Администратор узнаёт и о сервисе, который портал нашёл в реестре сам. По
  нажатию открывается страница сервиса, где ему задают категорию и владельца.
  Без этого сервис не появится в каталоге.
- О собственном обновлении портал тоже присылает уведомление. По нажатию
  открывается журнал изменений на разделе с этой версией.
- Уведомление можно отметить прочитанным, не открывая: наведите на него и
  нажмите галочку. Кнопка «Прочитать все» снимает счётчик разом. Прочитанные
  уведомления хранятся 90 дней, непрочитанные не удаляются.

### Изменено
- Если в реестре появилась новая версия сервиса, который публикует ваша команда,
  портал присылает уведомление. По нажатию открывается страница этой версии,
  чтобы описать её и отправить на согласование. Раньше об этом было сообщение на
  странице каталога, теперь его нет.
- Форма заказа объясняет отказ теми же словами, что и подсказка у поля. Вместо
  «length must be >= 3, but got 2» портал пишет «Не короче 3 символов», а поле,
  которое вы пропустили, подсвечивается само.
- Заказ, который портал не принял, объясняет причину по-русски. Неподходящее имя
  сервиса называет допустимые символы, а версия, пропавшая из реестра, просит
  выбрать доступную.
- В списке заказов и на самом заказе теперь восемь состояний вместо
  одиннадцати: черновик, сохраняем, разворачивается, работает, не работает,
  отклонено, удаляется, удалён. Фильтр над списком предлагает те же названия.
  Поддержка и администратор видят точное состояние в истории заказа, под
  «Подробно».
- Заказ больше не рассказывает про merge request, ветки и Git. Пока изменение не
  применилось, портал пишет, что сервис сохраняется. Ссылку на merge request
  видят только поддержка и администратор, в истории заказа под «Подробно».

### Исправлено
- Список заказов, страница заказа и уведомления снова обновляются сами после
  перезапуска портала или обрыва связи. Раньше обновления прекращались, и на
  странице оставались старые данные, пока её не перезагрузишь.

## [0.4.0] - 2026-08-18

Релиз про заполнение формы: поля сами говорят, чем их заполнить. В документации
появился разбор конструктора формы заказа.

### Добавлено
- В документации появился раздел «Конструктор формы заказа». В нём разобраны все
  блоки документа версии, с примерами и снимками экрана.
- Сервис может требовать, чтобы каждое изменение подтверждал человек, даже если
  портал настроен применять их сам. Владелец задаёт это в документе версии:
  `"approval": {"autoMerge": false}`. Портал всё равно готовит изменение так,
  чтобы проверяющему оставалось только подтвердить его.
- Портал сам настраивает в GitLab вебхук, которым GitLab сообщает ему о
  слияниях. Достаточно указать `GITLAB_WEBHOOK_URL`, а область задаётся в
  `GITLAB_WEBHOOK_SCOPE`. Раньше вебхук заводили руками после установки.

### Изменено
- Поля подсказывают, чем их заполнить: какие символы, какая длина, какой
  диапазон. Выполненные требования отмечаются прямо при вводе.
- Окно рисования связи на карте и окно добавления сервиса в каталог показывают
  ошибку прямо под полем, пока вы печатаете. Кнопка остаётся недоступной, пока
  значение не исправлено.
- На странице согласования публикаций заголовок остаётся на месте, прокручиваются
  только списки под ним. В таблице сервисов появились категория и дата
  последнего изменения, а действия над сервисом открываются прямо из строки.

### Исправлено
- Изменение сервиса больше не застревает из-за чужого изменения. Портал сводит
  правки по полям. Спросит только про поле, которое меняли оба.
- Версию, удалённую из реестра, больше нельзя заказать. Работающий на ней заказ
  продолжает работать, его можно перевести на доступную версию.
- Форма заказа открывается на рекомендуемой версии сервиса.

## [0.3.0] - 2026-08-14

Релиз про согласование версий и графы. Согласующий видит, что даёт версия
сервиса. Граф открывается у сервиса, который уже работает.

### Добавлено
- Граф открывается со страницы заказанного сервиса. Связи можно править и
  сохранять. Расположение блоков портал запоминает сам.
- Портал умеет следовать теме операционной системы. С неё начинается первый
  вход.
- К каждому релизу приложены образы портала и коллектора. Их можно установить
  там, где реестр образов недоступен.

### Изменено
- Версия сервиса согласуется на отдельной странице. Рядом видно форму заказа и
  страницу продукта. Отличия от действующей версии вынесены на отдельную
  вкладку.
- Тема выбирается одним переключателем. Значок в верхней панели показывает
  выбранную.
- История заказа занимает столько места, сколько нужно её событиям. На страницы
  она разбивается, только когда не помещается на экран.
- Общая информация о заказе подписана по-русски. Чарт, приложение в ArgoCD и
  конфигурация убраны под «Подробнее».
- Конфигурацию заказа открывает кнопка, названная по тому, что показывает.

### Исправлено
- Счётчик очереди на согласование больше не считает одну версию дважды.
- Найденный порталом чарт больше не приписывается администраторам. Пока его
  никто не взял, владелец не показывается.
- Сервис, который собирается на графе, больше нельзя заказать пустым. Портал
  просит нарисовать хотя бы одну связь.
- Заказ больше не застревает молча. Портал перестаёт пытаться и пишет в историю,
  что помешало.
- Заглушки, которые стоят на месте данных, видны в светлой теме.
- Неудачный вход возвращает на экран входа и объясняет, что делать.
- Вход не срывается из-за того, что страница простояла открытой несколько минут.

## [0.2.0] - 2026-08-10

Релиз выводит портал из демонстрационного режима. Подставные реестр, git-сервер
и система доставки убраны. Портал работает с настоящими и не запускается без их
адресов и токенов.

### Добавлено
- Несколько версий одного сервиса. У каждой версии своя форма заказа и своё
  согласование. В форме заказа версию можно выбрать.
- При обновлении предлагаются только версии, разрешённые владельцем сервиса.
- Карта сетевого взаимодействия. Вы рисуете стрелки между рабочими нагрузками, а
  карта пишет values для сервиса политик. Есть готовый пример.
- Заказ прямо с карты. Нарисованные стрелки разбиваются на заказы политик по
  namespace.
- Написанные руками values можно вставить в карту и дальше править мышкой.
- Вкладка «Граф» в форме заказа для сервисов, которые это поддерживают.
- Версия сервиса сама объявляет свой граф. Новая версия не ломает заказы,
  оставшиеся на прежней.
- Поиск и фильтр по категории в каталоге. Отфильтрованный каталог можно
  отправить ссылкой.
- Раздел «Админ»: обзор, очередь согласований, состояние платформы и категории
  каталога.
- Раздел «Поддержка»: заказы всех команд с фильтрами по команде и продукту.
- Сервис, найденный платформой в реестре, можно забрать себе. Для этого нужно
  выбрать категорию и владельца.
- У категорий появилась иконка. Продукт показывает иконку своего чарта.
- Документация в портале: каталог, заказ, статусы, публикация, роли, разделы и
  архитектура.
- На странице состояния платформы видно здоровье фоновых циклов и ссылку на
  Grafana.
- С настроенными вебхуками портал реагирует на слияние и загрузку чарта сразу,
  не дожидаясь следующего опроса.
- Сервис может сам называть свой namespace. Тогда поле в форме заказа исчезает,
  а портал показывает, какой namespace будет использован.
- Заказ с незавершённым изменением показывает баннер и не принимает новые
  изменения.
- Портал сообщает, когда часть платформы не отвечает. Значок в верхней панели
  показывает, что работает, а что нет.
- Действия, которые сейчас не пройдут, выключены с пояснением. Черновики при
  этом сохраняются.
- Страница «Конфигурация» в разделе «Админ». Пароли и токены не показываются,
  видно только, заданы они или нет.

### Изменено
- Экран входа показывает, что портал делает вместо ручной настройки сервисов.
  Вход занимает одно нажатие.
- Пустое изменение больше не принимается. Кнопка сохранения неактивна, пока
  ничего не изменилось.
- История заказа читается как список событий: строка на событие, группировка по
  дням и имя того, кто действовал.
- Ошибки написаны для читателя: понятный текст, подсказка и кнопка «Повторить».
  При недоступном реестре чартов портал прямо говорит, что заказать сейчас
  нельзя.
- Пока данные загружаются, портал показывает их заготовку вместо надписи
  «Loading». Когда данные приходят, страница не прыгает.
- Свёрнутое меню не сдвигает значки, они остаются на той же линии. Портал
  запоминает, какие разделы были раскрыты.
- Тёмные темы перекрашены: нейтральный чёрный фон и синий акцент.
- Формы в диалогах открываются панелью справа. Диалоги и меню анимированы.
- Ошибки полей сформулированы одинаково во всём портале. Числовые поля принимают
  только числа.
- «Чарты» теперь называются «Каталогом», «Согласованные» - «Опубликованными».
  Вкладки сервиса называются «Описание» и «Изменения».
- Редактор кода отдаётся самим порталом. Экраны публикации работают в сети без
  доступа в интернет.
- Метрики отдаются на отдельном порту (по умолчанию 2112, `METRICS_PORT`).
- Новая установка начинается с пустого каталога. Сервисы попадают в него
  регистрацией или находкой платформы.
- Статусы по умолчанию обновляются гибридно: вебхуки ускоряют портал, опрос
  остаётся страховкой.
- Действие «Синхронизировать» называется «Выкатить из Git» и действительно
  перечитывает Git.
- Журнал изменений на странице «О портале» показывает настоящий журнал проекта,
  а не забытую копию.
- Страница состояния платформы объясняет, что пользователи могут делать прямо
  сейчас и за что отвечает каждая внешняя система.

### Удалено
- Заглушки реестра чартов, Git и системы выкатки. Портал не запускается без
  адресов и токенов настоящих систем, настройте их перед запуском.
- Страница «Приложения», которая дублировала список заказов.
- Демонстрационные сервисы и категории в чистой установке.

### Исправлено
- Изменение, которое не удалось применить само, теперь видно. Раньше заказ
  молча зависал.
- Граф больше не выбрасывает незнакомые ему поля values. Values, которые он не
  может выразить, он отказывается рисовать, а не переписывает.
- Незаконченный view-документ ломает только панель предпросмотра, а не всю
  страницу редактора.
- Черновик больше не переименовывается при каждом сохранении. На занятое имя
  портал отвечает, какой заказ его держит.
- Колонки таблиц во view-документе добираются до вложенных списков и словарей.
- Версии с пре-релизным суффиксом сортируются правильно.
- Зависший запрос прерывается через 30 секунд с понятным сообщением.
- Сервисы, опубликованные версия за версией, видны в меню и в очереди
  согласования.
- Namespace заказа проверяется прямо при вводе.
- В тёмной теме поля ввода и списки больше не светятся белым.
- Панель редактирования справа затемняет страницу, а не накрывает её светлой
  пеленой.
- Картинка на экране входа больше не выцветает в светлой теме.

### Безопасность
- Сессии хранятся зашифрованными. Дамп кеша больше не отдаёт токены тех, кто
  сейчас в системе.
- Вход использует PKCE и сверяет полученный токен с начатым запросом. При
  продлении сессии права пересчитываются, поэтому отзыв группы действует сразу.
- Права администратора выдаются только за точное совпадение пути группы.
- Скрытый от пользователя сервис нельзя открыть, подобрав адрес.
- Скачанный чарт проверяется по контрольной сумме до распаковки.
- Ограничены размер запроса и заголовков, число потоков событий и простаивающие
  соединения. Внутренние сбои больше не возвращаются с подробностями наружу.
- Cookie сессии выдаётся с флагом Secure и явным сроком жизни (`COOKIE_SECURE`).
  Со стандартным секретом сессий портал не запускается.
- Отладочный вход отсутствует в боевой сборке.

## [0.1.0] - 2026-06-19

Первый релиз. Портал даёт командам самообслуживание: заказ сервисов из каталога,
GitOps-провижининг, согласование публикаций и вход через OIDC.

### Добавлено
- Заказ и обновление продуктов из каталога. Портал разворачивает их через GitOps
  в приложения Argo CD.
- Публикации чартов: категории, владельцы, view-документ и согласование.
- View-документы: динамические списки значений, вычисляемые колонки и проверка
  документа.
- Каталог: чарт добавляется по произвольному пути в Harbor, с проверкой
  комплектности.
- Конструктор: редактор view-документа и values.schema.json с предпросмотром
  заказа и продукта.
- Вход через OIDC и Keycloak. Роли: участник, администратор, поддержка и
  информационная безопасность.
- Метрики с дашбордом Grafana и структурные логи.
- Collector: снимает состояние Kubernetes в Valkey для консоли.
- Портал отдаёт встроенный интерфейс сам, без nginx. Есть страница «О портале» с
  версией сборки.
- Отдельные страницы документации.
- Сборка и релизы на GitHub Actions: проверки PR, тег и GitHub Release, сборка
  образов портала и коллектора.

[Unreleased]: https://github.com/awbait/console/compare/v0.6.0...HEAD
[0.6.0]: https://github.com/awbait/console/compare/v0.5.0...v0.6.0
[0.5.0]: https://github.com/awbait/console/compare/v0.4.0...v0.5.0
[0.4.0]: https://github.com/awbait/console/compare/v0.3.0...v0.4.0
[0.3.0]: https://github.com/awbait/console/compare/v0.2.0...v0.3.0
[0.2.0]: https://github.com/awbait/console/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/awbait/console/releases/tag/v0.1.0
