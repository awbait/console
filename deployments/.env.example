# Copy to deployments/.env and fill in before `make up-upstreams`.
#
# Long-lived Argo CD API token for the portal's real Argo client. Mint it with
# `make stand-up` (printed at the end) or `make stand-token`, then paste here.
ARGOCD_TOKEN=
